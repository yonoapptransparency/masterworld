## 2024-05-24 - Form Input Accessibility
**Learning:** Custom styled form inputs with icons often miss proper label associations which breaks screen reader support.
**Action:** Always verify `htmlFor` and `id` tags are paired correctly on form inputs.
