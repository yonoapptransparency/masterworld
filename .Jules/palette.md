## 2024-05-24 - Accessibility labels missing on UI buttons
**Learning:** Found multiple instances of icon-only or unlabelled buttons throughout the UI components lacking `aria-label` attributes, affecting keyboard navigation and screen readers.
**Action:** Always verify custom buttons include an accessible label, especially if they only display an icon (like Trash2, Plus, etc.) or just have generic visual labels.
